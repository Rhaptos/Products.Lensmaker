## Script (Python) "validate_lens_add_tags"
##bind container=container
##bind context=context
##bind namespace=
##bind script=script
##bind subpath=traverse_subpath
##parameters=lens_paths=[]
##title=validates properties for each selected open lens

# Nothing to do here at the moment

return state
